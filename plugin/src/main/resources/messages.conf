prefix = "<gradient:#4d8dff:#67d8c9><bold>Omniscience2</bold></gradient>"

page {
  header = "<gray>Page <white><page></white>/<white><pages></white></gray>"
}

search {
  empty = "<red>No matching records.</red>"
  stored = "<green>Stored <count> results.</green>"
}

rollback {
  summary = "<green><applied> applied</green><gray>, </gray><yellow><skipped> skipped</yellow>"
}

tool {
  enabled = "<green>Inspection tool enabled.</green>"
  disabled = "<yellow>Inspection tool disabled.</yellow>"
}

error {
  generic = "<red><message></red>"
}
