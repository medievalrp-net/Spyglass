# Contributing

Thanks for helping out.

Open an issue before a big change so we're on the same page. For anything you send:

- `./gradlew build` should pass, and new logic should come with a test.
- Keep `spyglass-api` stable, since other plugins build against it.

Opening a pull request means you agree to the [CLA](CLA.md).
